# Flask 核心框架（适配Railway）
Flask==3.0.0
# 解决跨域问题
flask-cors==4.0.0
# HTTP 请求（爬虫核心）
requests==2.31.0
# 网页解析（爬虫）
beautifulsoup4==4.12.0
# XML/HTML 解析（兼容新版Python，避免编译失败）
lxml>=5.3.0
# 通义千问 AI 接口
dashscope==1.14.0
# DuckDuckGo 搜索
duckduckgo-search==5.3.0
# OpenAI 兼容openai==1.13.3
# 环境变量管理
python-dotenv==1.0.0
gunicorn==21.2.0